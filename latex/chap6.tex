%!TEX root =  FnTarticle.tex

\chapter{Robotics Examples in GTSAM}

This chapter discusses our open-source implementation of the continuous-time estimation framework as an addition to GTSAM.

GTSAM is a widely used C++ library that leverages factor graphs to efficiently solve large-scale optimization problems in robotics and computer vision. One of the key contributions of our work is the introduction of Gaussian-process-based continuous-time (GP-CT) estimation into the existing GTSAM library. In this chapter, we will outline the essential functions that can be used to perform GP-CT estimation using our modified library and will provide some explanation of their implementation.

\section{GTSAM Primer}

Points that should be discussed are given below. 

\begin{itemize}
	\item discuss that GTSAM is essentially a large library of factors that can be assembled into any arbitrary factor graph.
	\item In general, practitioners either use existing factors, or build new factors for their given application. 
	\item Each factor has a set of keys that are associated with the random variables in the factor graph. At inference time, the keys correspond to the variables that are estimated.
	\item factors are defined seperately and added to a factor graph object
	\item along with initial value estimates, this object is then passed to an optimizer, which iteratively solves for the optimal estimates
	\item It is also possible to use the factor graph to compute the covariance of these variables, once the optimal values (the means) are known.
\end{itemize}

\section{Implementation Details}

In the following, we present implementation details to support the use of \ac{GTSAM} Gaussian process–based continuous-time estimation capabilities. We illustrate these concepts through a simple example showcasing the Gaussian Process Motion Prior Factor, along with both after-solve and during-solve interpolation functionalities. 

\subsection{Gaussian Process Motion Prior Factor}

As discussed in the preceding chapters, we have shown that, by assuming that the estimated trajectory of a robot is consistent with a given \ac{SDE}, the trajectory can be represented by a \ac{GP}. In turn, this \ac{GP} can be queried at any given time, both during and after the main optimization routine takes place.

To enforce this relationship in a factor graph setting, we must first define a \emph{motion model factor} between two robot states that are adjacent in time. We have implemented such a factor in \ac{GTSAM} for the \ac{WNOA} motion prior, leaving alternative motion models (e.g., \ac{WNOJ}, Singer) as future work. 
 
Recall that the state variable for the \ac{WNOA} prior consists of a both a pose and a velocity variable, where the velocity lives in the Lie algebra of the pose variable.
We have defined a custom C\texttt{++} struct (\lstinline[style=C++Style]|StateData|) to keep track of the pose, velocity, and timestamp associated with a particular state.\footnote{This struct has specific member functions that facilitate sorting states by time and comparing different states in a well-defined way. This allows efficient sorting for sets and maps, which are essential to some of the underlying operations in our implementation.}

Our implementation of the \ac{WNOA} requires two such state structures as well as the power spectral density matrix of the \ac{WNOA} prior. Alternatively, it can be defined directly on the \ac{GTSAM} keys as long as the time difference between the states is provided.

\begin{lstlisting}[style=C++Style]
  // Define states on (P1, V1) and (P2, V2), pose-vel pairs
  StateData state_1(P(1), V(1), 0.0);
  StateData state_2(P(2), V(2), timestep);
  
  // Define Power Spectral Density matrices
  Q_p2 = Vector4::Ones();
  Q_se3 = Vector6::Ones();

  // Define factor for 2D point
  WNOAMotionFactor<Point2> factor_P2(state_1, state_2, Q_p2);
  // Define factor for SE(3) pose
  WNOAMotionFactor<Pose3> factor_SE3(state_1, state_2, Q_se3);
\end{lstlisting}

Note that in this example, our factor is templated on a 2D point and an $SE(3)$ pose, but it also supports any vector space or Lie group defined in \ac{GTSAM}. 

\subsection{After-Main-Solve Querying}\label{sec:post-query-implem}
After the main solve, suppose we now wish to interpolate for intermediate poses in the solution. The Interpolator computes the means and covariances of the query points based on Section~\ref{sec:lie-after-query}.

\subsection{During-Main-Solve Querying}\label{sec:during-query-implem}

As mentioned in the preceding chapters, one key reason for using continuous-time estimation is that it can allow the practitioner to reduce the number of variables in a given optimization problem. We can treat a subset of the variables as interpolated states and use the methodology outlined in Section\ref{ch3:sec:during-solve} to eliminate them from the optimization, while still accounting for the effect of factors that are defined on these states. The independence approximation introduced in that chapter allows us to continue to treat each factor defined on these states separately (rather than lumping them together). Effectively, each factor defined on an interpolated state can be mapped to a new factor on the bordering states, removing the need to actually solve for the interpolated states. 

Our implementation allows a user to convert any \ac{GTSAM} factor graph into an equivalent graph where all the interpolated states have been removed from the optimization. The core element of this implementation is new interpolation factor that can be wrapped around any \ac{GTSAM} factor that is defined on one or more interpolated variables. This `wrapper' factor automatically maps the errors and Jacobians to the bordering states via the \ac{WNOA} interpolation equations. The resulting factor is defined only with respect to non-interpolated variables.

Mathematically, our wrapper factor implements the conversion from a measurement factor defined on an interpolated state, $p(\mbf{y}_n|\mbf{x}_{\tau_n})$, to the factor given in Equation~\eqref{eqn:interp-factor}, but with two key generalizations. First, the development shown in Section~\ref{ch3:sec:during-solve} applies to variables defined in a vector space, but our implementation can be used with variables defined on Lie groups, using the development shown in Chapter~\ref{chapLieGroups} for continuous-time systems on Lie groups. Second, Section~\ref{ch3:sec:during-solve} is demonstrated for unary factors on the interpolated variables, but our wrapper factor implementation can be applied to any kind of \ac{GTSAM} factor. As we show below, our framework can be applied to factors defined on any number of interpolated and non-interpolated variables.

\begin{lstlisting}[style=C++Style, caption={Setup for interpolated $SE(3)$ factors.}]
  #include <gtsam/nonlinear/WNOAInterpFactor.h>

  // Define a generic noise model
  const auto model = noiseModel::Diagonal::Sigmas(Vector6::Ones());
  // Define WNOA Power Spectral Density
  Q_se3 = Vector6::Ones();
  // Define SE(3) between factor from pose 1 to pose 3
  const auto between_factor = std::make_shared<BetweenFactor<Pose3>>(
      P(1), P(3), T_13, model);
  // Define SE(3) unary prior on pose 1
  const auto prior_pose_factor =
      std::make_shared<PriorFactor<Pose3>>(P(1), p1_se3, model);
  
  // Define timestep between poses
  timestep = 0.1; 
  // Define set of states to be interpolated
  // StateData struct defined as: StateData(PoseKey, VelocityKey, Time)
  set<StateData> interp = {StateData(P(1), V(1), timestep),
                           StateData(P(3), V(3), 3 * timestep)};
  // Define set of bordering states that will remain in the main solve.
  set<StateData> border = {StateData(P(0), V(0), 0.0),
                           StateData(P(2), V(2), 2 * timestep),
                           StateData(P(4), V(4), 4 * timestep)};
  // Define SE(3) Interpolation factor to wrap unary measurement
  const auto wrapped_unary = WNOAInterpFactor<Pose3>(prior_pose_factor, border, interp, Q_se3);
  // Define SE(3) Interpolation factor to wrap between measurement
  const auto wrapped_between = WNOAInterpFactor<Pose3>(between_factor, border, interp, Q_se3);

  // Create Graph
  NonlinearFactorGraph graph;
  graph.add(wrapped_unary);
  graph.add(wrapped_between);

\end{lstlisting}

In the listing above, we define two factors on the interpolated states for the first and third poses. These factors are then wrapped using our \lstinline[style=C++Style]{WNOAInterpFactor} factor (templated on $SE(3)$) and added to the graph. The resulting graph is defined only on the bordering states for the zeroth, second and fourth states (poses and velocities).

The factors above allow the user to exactly specify which states and factors they wish to convert. However, we also provide a function that automatically convert an entire factor graph into an equivalent, interpolated graph based on a list of interpolated states. This function (\lstinline[style=C++Style]{interpolateFactorGraph}) effectively carries out the operations shown in Figure~\ref{fig:gausselim3b}: it replaces any factors defined on interpolated states with wrapped, interpolated factors and adds \ac{WNOA} motion models factors between adjacent border states. It will also remove any \ac{WNOA} motion model factors defined on interpolated states. The resulting output graph will not be defined any of the interpolated state variables.

A factor graph built on the factors we have defined herein can be optimized in the same way as any other \ac{GTSAM} factor graph. Once optimization has completed, interpolated means and covariances can be obtained for any query time using the functions defined in the previous section. 

\begin{lstlisting}[style=C++Style]
  //       unary
  //         |
  //  0 ---- 1 ---- 2 ----- 3 ----- 4
  //  e      i      e       i       e
  //          --- between ---

  // Generate original graph
  NonlinearFactorGraph graph;
  graph.add(between_factor);
  graph.add(prior_pose_factor);
  // Convert the graph to an equivalent graph defined with interpolated factors
  auto graph_interp = interpolateFactorGraph<Pose3>(graph, border, interp, Q_se3);
  // Set up and run optimizer
  GaussNewtonOptimizer optimizer(graph_interp, values);
  Values result = optimizer.optimize()
  // Retrieve interpolated means
  Values result_interp = updateInterpValues<Pose3>(graph_interp, result, border, interp, Q_se3);
\end{lstlisting}

We must caution the reader regarding the covariances generated by the interpolated graph in two regards. First, as noted in Section~\ref{ch3:sec:during-solve}, the independence assumption treats correlated states as if they are not correlated, leading to overconfident estimates. The degree of this overconfidence depends on how correlated the interpolated states are as well as the number of interpolated factors. Second, covariances that are interpolated using the after-main-solve can sometimes exhibit a `bubbling' effect, especially when estimated states are particularly far apart in time or the power spectral density matrix has large eigenvalues.

Both of these phenomena are demonstrated in top two rows of Figure~\ref{fig:simple-se2-interp}. The figure provides a study of interpolation on a simple $SE(2)$ example. The ground truth poses follow a constant velocity arc trajectory and no-noise unary measurements were generated at 60 equidistant poses along the trajectory. The figure shows a comparison of the solutions obtained by solving the full factor graph (green) versus interpolating all but three states at the beginning, middle and end. 

\begin{figure}[ht!]
	\centering
	\includegraphics[width=\textwidth]{figs/a2_figs/interp_fig_annotated.png}
	\caption{A simple $SE(2)$ interpolation example demonstrating a full factor graph solve including all 60 states (green) compared to a reduced solve where only three of the 60 states are actually solved (blue). A unary factor is placed on each of the 60 states. The top row shows the estimated states in each case (with covariances). The bottom two rows show two different methods of interpolating the covariance of the interpolated states compared with the full solve. The middle row shows covariances interpolated via the after-main-solve equations, while the bottom row shows the covariances interpolated using the original graph with the interpolated means. The latter method provides covariances that match exactly with the full solve, while the former is subject to both overconfidence and `bubbling'.}
	\label{fig:simple-se2-interp}
\end{figure}

The final row of the figure demonstrates an alternative method for finding the covariances at the interpolated states. In this method, the means of the interpolated states are found using interpolation, but their associated covariances are generated from the Laplace approximation of the original graph about the computed means. This can be accomplished quite easily using existing \ac{GTSAM} functions:
\begin{lstlisting}[style=C++Style]
	// get interpolated means
	Values result = updateInterpValues<Pose3>(graph_interp, result_interp, border, interp, Q_se3);

    // build marginals object with the original graph and updated results
	Marginals marginals(graph, result);

	// get interpolated covariance for Pose 1 (interpolated)
	Matrix cov_p1 = marginals.marginalCovariance(P(1));
\end{lstlisting}


\subsection{Speeding up computation}

\begin{itemize}
	\item We know that we can speed up computation by making the independence approximation.
	\item An additional approximation that we can make is to omit the computation of the $\mbf{G} \mbf{\Sigma}_{\tau} \mbf{G}^T$ term in the measurement covariance computation. Since the computation of $\mbf{\Sigma}_{\tau}$ typically involves three inverses, it ends up being costly (there might be a better way to compute it).
	\item Approximating the Lie Group Jacobians as identity may be feasible when the change in pose between two consecutive border states is relatively small. This should lead to quite a bit of savings when computing derivatives.
\end{itemize}

\section{Implementation on Datasets}

We now turn to more sophisticated robotics estimation problems using real-world datasets to demonstrate the capabilities of the continuous-time estimation framework across one-dimensional, two-dimensional, and three-dimensional scenarios.
While high-level descriptions are provided in the following subsections, readers are encouraged to explore the dataset examples themselves, which are available in the GTSAM open-source repository along with the plotting scripts used to generate the graphs in this section.

\subsection{1D: Giant Glass of Milk Dataset}

In our first example dataset, we are investigating a linear one-dimensional problem, in which a mobile robot is driving back and forth in a straight line.
The experimental setup is shown in Figure~\ref{fig:giant_glass_of_milk}, consisting of the robot, two fixed guiding rails as well as a large cylinder.
Both the robot and the cylinder are equipped with reflective markers, which are tracked by a ten-camera motion capture system, providing ground-truth positions with millimeter-level accuracy.
Additionally, the robot is equipped with a laser rangefinder (Hokuyo URG-04LX) that provides 681 range measurements over a 240° horizontal field of view, centered straight ahead, as well as wheel odometers for measuring the robot's speed.

\begin{figure}[ht!]
	\centering
	\includegraphics[width=0.8\textwidth]{GiantGlassOfMilk.png}
	\caption{A mobile robot driving between rails. It is equipped with an odometer to measure translational speed and a laser rangefinder to measure the range to the large white cylinder.}
	\label{fig:giant_glass_of_milk}
\end{figure}


\begin{figure}[ht!]
	\centering
	\includegraphics[width=1\textwidth]{a1_example_results.pdf}
	\caption{Robot state estimate over a subset of its trajectory for different measurement frequencies. Position and velocity measurements are provided only every 2.5 seconds, 5 seconds or 7.5 seconds, respectively. Between these times, the estimate relies solely on the Gaussian process prior. The plots show the mean estimates with 3-$\sigma$ uncertainty envelopes.}
	\label{fig:giant_glass_of_milk_results}
\end{figure}

During data collection, the robot was driven back and forth along the two guiding rails for 20 minutes, logging laser rangefinder and odometer data at 10 Hz, while ground-truth positions were recorded at 70 Hz.
All measurement data were temporally aligned in a post-processing step using linear interpolation.
Moreover, the distance between the robot and the cylinder was extracted from each rangefinder scan by exploiting the cylinder's circular shape.
This eliminates the need to process the raw laser scans directly, allowing the use of a simplified range measurement instead.
Given the known location of the cylinder, the range measurement can be treated as a noisy observation of the robot’s position.
The noise of each sensor was characterized using the ground-truth data provided by the motion capture system.

Using our continuous-time state estimation framework, we now seek to estimate its position and velocity throughout it's approx. 280~m long trajectory using only the noisy position and velocity measurements.
For this, we set our problem up using GTSAM, defining estimation states, including position and velocity, every 0.1 seconds, while adding relative WNOA motion prior factors between them.
Position and velocity measurements are included as straightforward unary factors on the states.
Fig.~\ref{fig:giant_glass_of_milk_results} shows the robot estimate at 10 Hz using our framework for different measurement frequencies.
Three different cases are shown with meausrements being available every 2.5, 5 and 7.5 seconds, respectively.
It can be seen that our approach achieves good estimation accuracy even with relatively sparse data, leveraging the motion prior to produce a continuous estimate over time.
As expected, accuracy improves with a higher number of measurements.
Nonetheless, even with sparse data, the physics-based prior enables close approximations of the ground truth.

\textcolor{red}{Sven: Add short paragraph on comparison when using interpolation vs. no interpolation}


\begin{figure}[ht!]
	\centering
	\includegraphics[width=1\textwidth]{a1_example_interpolation.pdf}
	\caption{Comparison of incorporating discrete states into the factor graph at every 0.1 s versus only every 5 s (when measurements are available). In the latter case, intermediate states are recovered using our post-query interpolation scheme. For this one-dimensional linear example, both approaches yield identical solutions.}
	\label{fig:giant_glass_of_milk_interpolation}
\end{figure}



\subsection{2D: Lost in the Woods Dataset}

The second dataset that we introduce here is geared towards estimation on $SE(2)$ such as either localization or \ac{SLAM}. The setup consists of a wheeled robot exploring a flat space amongst a forest of fixed plastic tubes that can be used as landmarks. The robot is equipped with a laser rangefinder (Hokuyo URG-04LX sensor) scans with 681 range measurements over a 240 degree horizontal field of view centered on straight ahead. The robot also has wheel encoders that measure linear and angular speeds. 

Sensor data has been sanitized to facilitate the study of back-end estimation routines. In particular, biases have been removed and rangefinder measurements have been converted to bearing-range measurements to landmarks, with known data association. The ground truth position and orientation of the rangefinder origin on the robot and landmark locations are tracked via a Vicon motion capture system. All data in the dataset has been synchronized at 10 Hz (approximately). Images of the setup and ground truth trajectory for the dataset are shown in Figure~\ref{fig:litw-setup}.

\begin{figure}[ht!]
	\centering
	\includegraphics[width=\textwidth]{a2_figs/litw_fig1_setup_gt.png}
	\caption{Dataset setup and ground truth trajectory (in 2D). Image \textbf{a.} shows the setup of the robot and the plastic cylinders that serve as the landmark while image \textbf{b.} highlights the 10 camera Vicon system used to obtain the ground truth positions and orientations of the robot throughout the dataset. Plot \textbf{c.} shows the full trajectory of the robot throughout the dataset. The start and end positions are marked with a green and red star, respectively.}
	\label{fig:litw-setup}
\end{figure}

Figure~\ref{fig:litw-gtsam-solve} shows a segment of the trajectory that has been optimized via \ac{GTSAM} with all available bearing-range and wheel odometry measurements. In this and all other cases, the optimization was initialized using a roll-out of the wheel odometry measurements. Note that with the available data, the robot can be localized to a high degree of accuracy.

\begin{figure}[ht!]
	\centering
	\includegraphics[width=\textwidth]{a2_figs/litw_fig2_solve.png}
	\caption{\ac{GTSAM} localization solution of a segment (timestep 1000 to timestep 6000) of the Lost in the Woods dataset using both bearing-range and wheel odometry measurements. The ground truth trajectory is shown in green, while the optimal trajectory is shown in blue. Covariance ellipsoids based on three standard deviations are plotted in blue for the estimated values.}
	\label{fig:litw-gtsam-solve}
\end{figure}

We now turn our attention to the application of the methods described herein to this dataset. We make the state estimation problem more challenging by artificially limiting the available measurements:  bearing-range measurements with range greater than 1 m are removed and the wheel odometry measurements are completely removed (though we still use odometry to initialize the trajectory). We then apply our \ac{WNOA} motion model factor between poses that are adjacent in time. The resulting trajectory is shown in Figure~\ref{fig:litw-loc-wnoa}. Note that the estimated trajectories deviate from the ground truth in regions where there are fewer available bearing-range measurements. In these regions, the estimate relies entirely upon the \ac{WNOA} prior, leading to an increase in uncertainty (covariance ellipsoids grow). 

\begin{figure}[ht!]
	\centering
	\includegraphics[width=\textwidth]{a2_figs/litw_fig3_full.png}
	\caption{\ac{GTSAM} localization solution of a segment (100 seconds to 250 seconds) of the Lost in the Woods dataset with bearing-range measurements limited to 1 m and \ac{WNOA} motion model factors between adjacent states (no odometry). The ground truth trajectory is shown in green, while the estimated trajectory is shown in blue. Covariance ellipsoids based on three standard deviations are plotted in blue for the estimated values. In regions with few bearing-range measurements (between 0 and 4 m along the x-axis), estimate uncertainty increases since it relies only on the \ac{WNOA} motion prior.}
	\label{fig:litw-loc-wnoa}
\end{figure}

Figure~\ref{fig:litw-loc-wnoa-interp} shows the exact same problem as Figure~\ref{fig:litw-loc-wnoa}, but only one in every 30 trajectory states are actually computed in the main solve, with the remainder being interpolated. Bearing-range factors on interpolated states were converted to new factors on the remaining states by applying our \lstinline[style=C++Style]|WNOAInterpFactor| factor (described in Section~\ref{sec:during-query-implem}). We would like to draw the reader's attention to the fact that, despite this aggressive interpolation scheme, we are still able to recover a trajectory that are \emph{almost identical} to the full solve trajectory. The similarity of the covariances suggest that the overconfidence and bubbling issues described above for interpolation may not always be apparent for practical examples.

\begin{figure}[ht!]
	\centering
	\includegraphics[width=\textwidth]{a2_figs/litw_fig3_interp.png}
	\caption{\ac{GTSAM} localization solution for the same setup as in Figure~\ref{fig:litw-loc-wnoa}, but with interpolation. The main solve only optimizes every $30^{th}$ state in the trajectory (i.e., every 3 seconds), with the remainder of the states being interpolated. After the main solve, the interpolated states were added back to the trajectory using after-main-solve querying described in Section~\ref{sec:post-query-implem}. The ground truth trajectory is shown in green, while the estimated trajectory is shown in blue. Covariance ellipsoids are plotted based on three standard deviations. Non-interpolated states are represented with larger, darker frames and darker ellipsoids with borders. Note that the interpolated solution matches well with the non-interpolated solution in Figure~\ref{fig:litw-loc-wnoa}.}
	\label{fig:litw-loc-wnoa-interp}
\end{figure}

In Figure~\ref{fig:litw-SLAM-wnoa-interp}, we turn our attention to solving interpolated SLAM on this dataset. That is, we allow the landmarks to be variables in the problem. As in the previous example, we only keep one of every thirty states in the main solve, interpolating the remaining states and converting factors on interpolated states to \lstinline[style=C++Style]|WNOAInterpFactor| factors. Note that this experiment demonstrates the ability of our custom interpolation to handle factors that are defined on a combination of interpolated and non-interpolated states (the landmarks are not interpolated). 

In this example, we extract the covariances of the interpolated states using the alternative method described in Section~\ref{sec:during-query-implem} (i.e., interpolating the mean values and then using the original factor graph to obtain covariances). Note that both the trajectory (blue) and the landmarks (red) track the ground truth (green) well.

\begin{figure}[ht!]
	\centering
	\includegraphics[width=\textwidth]{a2_figs/litw_fig4_interp.png}
	\caption{\ac{GTSAM} SLAM solution with interpolation for the robot trajectory between 200 seconds and 500 seconds. The main solve only optimizes every $30^{th}$ state in the trajectory (i.e., every 3 seconds), with the remainder of the states being interpolated. The ground truth trajectory is shown in green, while the estimated trajectory is shown in blue. Covariance ellipsoids are plotted based on three standard deviations. Non-interpolated states are represented using darker ellipsoids with borders.}
	\label{fig:litw-SLAM-wnoa-interp}
\end{figure}

\subsection{3D: Starry Night Dataset}
We now turn to a 3D dataset to demonstrate the above concepts in SE(3). The setup consists of a sensor head that contains an inertial measurement unit (IMU) rigidly attached to a stereo camera. The sensor head is being moved around above a field of reflective markers acting as landmarks, and groundtruth poses of the sensor head is collected using a motion capture system. The localization objective is to estimate the trajectory of the sensor head. See \textit{here} for a video of the data collection process.

To focus more on the factor graph theory (reword this), this dataset within the GTSAM repository has already been sanitized. For odometry, we have derived the sensor head's (unbiased) linear and angular velocities based on the raw IMU data. For measurements, we have extracted stereo features from the images, and have associated these features with the landmarks based on groundtruth data. The odometry and the groundtruth poses have been interpolated at the measurement times.

We follow a similar procedure as for the datasets above. We first compare localization using an odometry motion prior, with localization using a WNOA prior.  The results are... We also look at interpolation... We encourage interested readers to try out other interpolation configurations for localization, and even SLAM.
